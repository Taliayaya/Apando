import styled from 'styled-components'
import GroupIcon from '@mui/icons-material/Group'

const ServerIcon = styled(GroupIcon)`
    color: ${(props) => props.theme.font_color};
`

export { ServerIcon }
